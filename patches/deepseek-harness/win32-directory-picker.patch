diff --git a/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/index.js b/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/index.js
@@ -71,10 +71,23 @@ async function closeThreadWindows(threadId) {
 * @returns the spawned child process.
 */
 function spawnDialogWorker(data) {
+	const nodeExecutable = process.env.MINKE_NODE_EXECUTABLE;
+	if (nodeExecutable === void 0 || nodeExecutable === "") throw new Error("Minke managed Node executable is unavailable");
+	const bootstrap = process.env.MINKE_NODE_BOOTSTRAP;
+	if (bootstrap === void 0 || bootstrap === "") throw new Error("Minke managed Node bootstrap is unavailable");
 	const env = {
-		...process.env,
-		DSH_DIALOG_TITLE: data.title
+		...process.env
 	};
+	for (const name of [
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]) for (const key of Object.keys(env)) if (key.toUpperCase() === name) delete env[key];
+	env.ELECTRON_RUN_AS_NODE = "1";
+	env.DSH_DIALOG_TITLE = data.title;
 	const stdio = [
 		"ignore",
 		"inherit",
@@ -82,12 +95,14 @@ function spawnDialogWorker(data) {
 		"ipc"
 	];
 	/* v8 ignore next 3 -- the built-output arm: tests always run unbuilt (src/) */
-	if (!import.meta.url.endsWith(".ts")) return spawn(process.execPath, [fileURLToPath(new URL("./worker.cjs", import.meta.url))], {
+	if (!import.meta.url.endsWith(".ts")) return spawn(nodeExecutable, ["--require", bootstrap, fileURLToPath(new URL("./worker.cjs", import.meta.url))], {
 		env,
 		stdio,
 		windowsHide: true
 	});
-	return spawn(process.execPath, [
+	return spawn(nodeExecutable, [
+		"--require",
+		bootstrap,
 		"--import",
 		import.meta.resolve("tsx/esm"),
 		fileURLToPath(new URL("./win32-dialog-worker.ts", import.meta.url))
diff --git a/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/worker.cjs b/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/worker.cjs
--- a/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/worker.cjs
+++ b/node_modules/@deepseek-ai/dsh-host-directory-picker-native/lib/worker.cjs
@@ -15,14 +15,9 @@
 /**
-* Read a NUL-terminated UTF-16 string at a native address. koffi's
-* `_Out_ void **` out-params surface a raw address, and
-* `koffi.decode(addr, 'str16')` would dereference it as a pointer — crash
-* on real Windows — so view the memory directly instead.
+* Copy a NUL-terminated UTF-16 string from a native address without creating
+* an external ArrayBuffer, which is unsafe in the packaged Electron runtime.
 */
 function readUtf16(koffi, address) {
-	const bytes = Buffer.from(koffi.view(address, 32768));
-	let end = 0;
-	while (end + 1 < bytes.length && !(bytes[end] === 0 && bytes[end + 1] === 0)) end += 2;
-	return bytes.toString("utf16le", 0, end);
+	return koffi.decode.string16(address);
 }
 const COINIT_APARTMENTTHREADED = 2;
 const CLSCTX_INPROC_SERVER = 1;
@@ -205,9 +205,10 @@ if (title === "") throw new Error("win32-dialog-worker: DSH_DIALOG_TITLE is requ
 if (process.send === void 0) throw new Error("win32-dialog-worker must run as a child process with an IPC channel");
 const send = process.send.bind(process);
 const post = (message) => {
+	const isTerminal = message.kind !== "showing";
 	/* v8 ignore next 3 -- disconnect needs a live IPC channel the unit lane must not sever (built-worker.e2e.ts owns the real close path). */
 	send(message, () => {
-		if (process.connected) process.disconnect();
+		if (isTerminal && process.connected) process.disconnect();
 	});
 };
 /* v8 ignore next 3 -- the handler exits(0), which would kill the unit lane; built-worker.e2e.ts owns the real disconnect lifecycle. */
diff --git a/node_modules/@deepseek-ai/dsh-sandbox-local/lib/index.js b/node_modules/@deepseek-ai/dsh-sandbox-local/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-sandbox-local/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-sandbox-local/lib/index.js
@@ -524,11 +524,13 @@ var LocalSandboxProvider = class extends SandboxProvider {
 	windowsAclRunnerInvocation() {
 		const override = this.internals.windowsAclRunnerArgs;
 		if (override !== void 0) return override;
+		const nodeExecutable = process.env.MINKE_NODE_EXECUTABLE;
+		if (nodeExecutable === void 0 || nodeExecutable === "") throw new Error("Minke managed Node executable is unavailable");
 		const builtEntry = this.internals.windowsAclRunnerEntry ?? fileURLToPath(import.meta.resolve("@deepseek-ai/dsh-sandbox-windows-acl/runner"));
-		if (existsSync(builtEntry)) return [process.execPath, builtEntry];
+		if (existsSync(builtEntry)) return [nodeExecutable, builtEntry];
 		const sourceEntry = fileURLToPath(import.meta.resolve("@deepseek-ai/dsh-sandbox-windows-acl/src/runner.ts"));
 		return [
-			process.execPath,
+			nodeExecutable,
 			"--import",
 			"tsx/esm",
 			sourceEntry
