diff --git a/node_modules/@deepseek-ai/dsh-client-connection/lib/index.js b/node_modules/@deepseek-ai/dsh-client-connection/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-client-connection/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-client-connection/lib/index.js
@@ -513,6 +513,13 @@ var HostConnectionService = class extends Service {
 		this.trustedHosts = trustedHosts;
 		this.browserAuth = browserAuth;
 	}
+	/** Atomically replace the deployment authorities used by live routes. */
+	replaceTrustedHosts(trustedHosts) {
+		for (const entry of trustedHosts) assertTrustedAuthority(entry);
+		this.trustedHosts.splice(
+			0, this.trustedHosts.length, ...new Set(trustedHosts)
+		);
+	}
 	/** Generic channel registry scoped to the Context reading this service. */
 	get rpc() {
 		const owner = this.ctx;
@@ -692,7 +699,7 @@ const Config = z.object({
 * @param config - resolved plugin config (schema defaults applied).
 */
 async function apply(ctx, config) {
-	const trustedHosts = config?.trustedHosts ?? [];
+	const trustedHosts = [...(config?.trustedHosts ?? [])];
 	const cookieMaxAgeDays = config?.cookieMaxAgeDays ?? 30;
 	const maxRequestBodyBytes = config?.maxRequestBodyBytes ?? 314572800;
 	for (const entry of trustedHosts) assertTrustedAuthority(entry);
